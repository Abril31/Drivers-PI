import React from "react";
const SearchBar = () => {
  return (
    <div>
      <form>
        <input type="text" />
        <button>Search</button>
      </form>
    </div>
  );
};

export default SearchBar;
